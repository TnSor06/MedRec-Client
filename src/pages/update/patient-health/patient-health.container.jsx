import React from "react";
import PatientHealth from "./patient-health.component";

const PatientHealthContainer = (props) => {
  return <PatientHealth {...props}></PatientHealth>;
};

export default PatientHealthContainer;
